services:
  - type: web
    name: hostel-management
    env: python
    branch: main
    buildCommand: pip install --upgrade pip setuptools wheel && pip install -r requirements.txt
    startCommand: gunicorn hostel_project.wsgi:application --bind 0.0.0.0:$PORT
